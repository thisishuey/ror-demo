json.extract! job,
              :id,
              :title,
              :company,
              :city,
              :state,
              :position,
              :rate,
              :favorite,
              :expired,
              :score,
              :description,
              :created_at,
              :updated_at
json.url job_url(job, format: :json)
